const state = {
	detailData:[],
	bottomData:[]
};
import {
	getDetailList
} from '@/api/index.js';

const mutations = {
	updateList(state,payload){
		
	}
};

const actions = {
};

const getters = {

};

export default {
	namespaced: true,
	state,
	mutations,
	actions
};